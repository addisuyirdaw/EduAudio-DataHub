# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

This project targets **Expo SDK 57** (React Native 0.86, React 19.2). Key pinned packages:
- `expo` ^57.0.1
- `expo-av` ~16.0.8 (expo-av 16.x API)
- `expo-speech` ~57.0.0
- `expo-document-picker` ~57.0.0
- `expo-file-system` ~57.0.0
- `expo-haptics` ~57.0.0

Always verify SDK-specific APIs (audio mode config, permissions, speech) against the Expo SDK 57 / expo-av 16 docs before writing or modifying code.
